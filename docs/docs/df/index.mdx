import DocCardList from '@theme/DocCardList';

# DF Module

A module for data fusion (low-level or mid-level).

# Members

<DocCardList />

# UML

```mermaid
classDiagram
    class Table {
        +str file_path
        +str sheet_name
        +str preprocessing
        __init__(file_path, sheet_name, preprocessing)
    }

    class DFSettings {
        +GraphOutput output
        __init__(output)
    }

    class DF {
        +DFSettings settings
        +Table[] tables
        +DFDataModel | None fused_data
        +fuse()
        -_snv()
        +export_data(export_path: str)
        __init__(settings, tables[])
        -_perform_feature_selection(table: Table, data_model: BaseDataModel) BaseDataModel
        -_preprocess_table(table, x)
    }

    class DFDataModel {
        +x_data: pd.DataFrame
        +x_train: pd.DataFrame
        +y: np.ndarray
        +tables: list[tuple[table, BaseDataModel]]
        __init__(x_data, x_train, y)
    }

    DF *-- DFDataModel
    DF *-- Table
    DF *-- DFSettings
    DFDataModel *-- Table
```